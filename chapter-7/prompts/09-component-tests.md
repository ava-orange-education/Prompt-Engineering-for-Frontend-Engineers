# Prompt — Component Tests

```
Generate React Testing Library tests in src/components/ui/KpiCard.test.tsx.

Tests to write:
  KpiCard:
    - Renders the title and value
    - Shows a green badge when change > 0
    - Shows a red badge when change < 0
    - Shows loading skeleton when loading=true (no title/value visible)
    - Has correct aria-label combining title and value

Use @testing-library/react and @testing-library/jest-dom matchers.
Do not test CSS animation; test class presence only.
```
